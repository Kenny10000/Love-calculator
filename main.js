function calculate(){
if (playerName.value == "" || loveName.value == ""){
err.innerHTML = "All input filed are required" 
err.style.display = "block"
    // setTimeout(() =>{
    //     err.style.display = "none"
    // },5000
    // )
}
}